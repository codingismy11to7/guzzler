name: Get Version
description: Get server version for Docker
outputs:
  version:
    description: The server version
    value: version

runs:
  using: composite
  steps:
    - shell: bash
      run: echo "version=$(npx tsx scripts/printVersion.mts)" >> "$GITHUB_OUTPUT"
